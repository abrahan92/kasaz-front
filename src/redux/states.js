export const baseInitialState = {
  loading: false,
};

export const propertyInitialState = {
  properties: [],
  filters: {
    min_price: null,
    max_price: null,
    min_size: null,
    max_size: null,
    rooms: null,
  },
};
